package com.aitc.expensetrackerandroid.ui.theme

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import com.aitc.expensetrackerandroid.R

/**
 * Loaded via [R.font.noto_sans] XML so Android maps TextStyle.fontWeight to the
 * correct static file at runtime (Compose Preview can synthesize weights; devices cannot).
 */
val AppFontFamily = FontFamily(Font(R.font.noto_sans))
